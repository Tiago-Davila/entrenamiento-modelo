# Dependencias instalables de Eva / LSA64 v2.
# Python soportado: 3.9 a 3.13 (recomendado: Python 3.12).
#
# Instalar desde la raiz del repositorio con:
#   python -m pip install --upgrade pip setuptools wheel
#   python -m pip install -r REQUERIMIENTOS_LSA64.txt
#
# El archivo holistic_landmarker.task no es un paquete de Python. Debe
# descargarse aparte y colocarse en lsa64/assets/.

mediapipe==0.10.33
tensorflow-cpu>=2.17,<3.0
numpy>=2.0,<3.0
scikit-learn>=1.4,<2.0
tqdm>=4.66,<5.0
